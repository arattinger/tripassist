var OfflineMap = (function() {


    var self = {};
    self.addRectToCache = function(lat1, lon1, lat2, lon2, maxZoomLevel, callback) {
        alert('add rect to cache');
    }

    self.render = function(lat, lon, zoomLevel, ctn) {
        alert('rendering map');
    }
    return self;
})();
